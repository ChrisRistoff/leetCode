import { maxProfit, maxProfit2 } from "../../easy/bestTimeToBuyStock";

describe('maxProfit', () => {
  it('should return the maximum profit from buying and selling stocks', () => {
    expect(maxProfit([7, 1, 5, 3, 6, 4])).toBe(5);
  });

  it('should return the maximum profit from buying and selling stocks', () => {
    expect(maxProfit([7, 6, 4, 3, 1])).toBe(0);
  });

  it('should return the maximum profit from buying and selling stocks', () => {
    expect(maxProfit([7, 1, 5, 3, 6, 4])).toBe(5);
  });

  it('should return the maximum profit from buying and selling stocks', () => {
    expect(maxProfit([7, 6, 4, 3, 1])).toBe(0);
  });
});

describe('maxProfit2', () => {
  it('should return the maximum profit from buying and selling stocks', () => {
    expect(maxProfit2([7, 1, 5, 3, 6, 4])).toBe(5);
  });

  it('should return the maximum profit from buying and selling stocks', () => {
    expect(maxProfit2([7, 6, 4, 3, 1])).toBe(0);
  });

  it('should return the maximum profit from buying and selling stocks', () => {
    expect(maxProfit2([7, 1, 5, 3, 6, 4])).toBe(5);
  });

  it('should return the maximum profit from buying and selling stocks', () => {
    expect(maxProfit2([7, 6, 4, 3, 1])).toBe(0);
  });
});
